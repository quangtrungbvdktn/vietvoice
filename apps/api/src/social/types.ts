export class PublishError extends Error{constructor(public code:"PUBLISH_PERMISSION_REQUIRED"|"TOKEN_EXPIRED"|"PUBLISH_FAILED"){super(code)}}export interface PublishRequest{videoPath:string;caption:string;hashtags:string[];visibility:"private"|"public";scheduledAt?:string}export interface PublishProvider{publish(input:PublishRequest):Promise<{platformPostId:string;publicUrl?:string}>}
